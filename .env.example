# ─────────────────────────────────────────
# NOEMA — Environment Configuration
# ─────────────────────────────────────────
# Copy this file to apps/api/.env and fill in your API keys.
#
# For Cognee: also copy to apps/cognee_service/.env
# ─────────────────────────────────────────

# ─── Gemini 3 API (Required for real LLM mode) ───
# Get your key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Model configuration (defaults shown — override if needed)
# GEMINI_MODEL=gemini-3-flash-preview
# GEMINI_VISION_MODEL=gemini-3-pro-image-preview

# ─── OpenAI API (Optional — enables Cognee semantic memory) ───
# Used by Cognee for embeddings (LanceDB + Kuzu)
# OPENAI_API_KEY=your_openai_api_key_here

# ─── Cognee Service (Optional) ───
# URL of the Cognee Python service
# COGNEE_SERVICE_URL=http://localhost:8100

# ─── NOEMA API Server ───
# Port for the NOEMA backend API
# NOEMA_API_PORT=8200

# ─── Test Credentials (Optional — for secure demo) ───
# These are injected into NOEMA's decision context for form filling.
# They are NEVER shown in narration, logs, or the frontend.
# TEST_USERNAME=testuser@example.com
# TEST_PASSWORD=your_test_password_here

# For additional credential fields (JSON format):
# TEST_CREDENTIALS_JSON={"api_token":"abc123","2fa_code":"123456"}
